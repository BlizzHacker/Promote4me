import fs from 'fs';

const has = (s, x) => s.includes(x);
console.log('Applying Promote4.me 3.0.0 provider playbooks, carrier tracking, and digital delivery workflow...');

const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
pkg.version = '3.0.0';
for (const script of ['apply-2.9.9-mobile-workflow-polish.mjs', 'apply-3.0.0-provider-playbooks.mjs']) {
  if (!pkg.scripts.prebuild.includes(script)) pkg.scripts.prebuild += ' && node scripts/' + script;
}
fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');

let server = fs.readFileSync('server/index.js', 'utf8');
server = server.replace(/version: '[^']+'/g, "version: '3.0.0'");
server = server.replaceAll('authRequired', 'requireAuth');

if (!has(server, 'const P4ME_PROVIDER_PLAYBOOKS_300')) {
  const endpoints = `
const P4ME_PROVIDER_PLAYBOOKS_300 = {
  uber_eats: { access:'Requires approved Uber Direct / Eats API access', required:[['base_url','Uber API base URL'],['api_key','Client ID or API key'],['api_secret','Client secret'],['account_id','Merchant/store ID']], can:['Import delivery/order requests','Create proof-ready courier packets','Map statuses into Promote4.me','Capture GPS/photo evidence outside Uber'], webhook:'/api/providers/webhook/uber_eats', statuses:{created:'Assigned',accepted:'In Progress',picked_up:'In Progress',delivered:'Delivered',cancelled:'Exception'}, template:{provider_order_id:'UBER-1001',customer:{name:'Uber Test Customer',phone:'555-0100'},address:'302 E 4th St, Joplin, MO',type:'Food Delivery',instructions:'Confirm pickup, deliver order, capture proof.'}},
  doordash: { access:'Requires approved DoorDash Drive / Marketplace API access', required:[['base_url','DoorDash API base URL'],['api_key','Developer ID / Key'],['api_secret','Signing secret / secret'],['account_id','Business or store ID']], can:['Create DoorDash proof packets','Receive webhook order events','Map Dasher-style status events','Store order-level proof checklist'], webhook:'/api/providers/webhook/doordash', statuses:{created:'Assigned',dasher_confirmed:'In Progress',picked_up:'In Progress',delivered:'Delivered',cancelled:'Exception'}, template:{provider_order_id:'DD-1001',customer:{name:'DoorDash Test Customer',phone:'555-0101'},address:'302 E 4th St, Joplin, MO',type:'Food Delivery',instructions:'Verify order handoff and capture proof.'}},
  instacart: { access:'Requires approved Instacart Connect / retailer API access', required:[['base_url','Instacart API base URL'],['api_key','API key'],['api_secret','API secret'],['account_id','Retailer/store ID']], can:['Create grocery proof packets','Track shopping/delivery phases','Attach item checklist','Capture substitution notes as proof'], webhook:'/api/providers/webhook/instacart', statuses:{created:'Assigned',shopping:'In Progress',out_for_delivery:'In Progress',delivered:'Delivered',cancelled:'Exception'}, template:{provider_order_id:'IC-1001',customer:{name:'Instacart Test Customer'},address:'302 E 4th St, Joplin, MO',type:'Grocery Delivery',instructions:'Verify grocery items, substitutions, delivery proof.'}},
  grubhub: { access:'Requires approved Grubhub API/partner access', required:[['base_url','Grubhub API base URL'],['api_key','API key'],['api_secret','API secret'],['account_id','Restaurant/store ID']], can:['Receive order webhooks','Create restaurant delivery work packets','Map pickup/delivery statuses','Collect dispute-ready proof'], webhook:'/api/providers/webhook/grubhub', statuses:{created:'Assigned',accepted:'In Progress',picked_up:'In Progress',delivered:'Delivered',cancelled:'Exception'}, template:{provider_order_id:'GH-1001',customer:{name:'Grubhub Test Customer'},address:'302 E 4th St, Joplin, MO',type:'Food Delivery'}},
  postmates: { access:'Postmates is now part of Uber; use approved Uber/Postmates API access where available', required:[['base_url','API base URL'],['api_key','API key/client ID'],['api_secret','Secret'],['account_id','Account ID']], can:['Create courier packets','Map pickup/dropoff statuses','Keep historical Postmates source labeling'], webhook:'/api/providers/webhook/postmates', statuses:{created:'Assigned',pickup:'In Progress',dropoff:'Delivered',cancelled:'Exception'}, template:{provider_order_id:'PM-1001',customer:{name:'Postmates Test Customer'},address:'302 E 4th St, Joplin, MO',type:'Courier'}},
  walmart_spark: { access:'Requires approved Walmart/Spark partner API access', required:[['base_url','Walmart/Spark API base URL'],['api_key','API key'],['api_secret','Secret'],['account_id','Store/account ID']], can:['Create retail delivery packets','Attach package/item checklist','Track pickup/dropoff proof'], webhook:'/api/providers/webhook/walmart_spark', statuses:{created:'Assigned',picked:'In Progress',delivered:'Delivered',failed:'Exception'}, template:{provider_order_id:'SPARK-1001',customer:{name:'Spark Test Customer'},address:'302 E 4th St, Joplin, MO',type:'Retail Delivery'}},
  amazon_flex: { access:'Requires approved Amazon/merchant logistics API access; not a public open API for all accounts', required:[['base_url','Amazon/logistics API base URL'],['api_key','API key'],['api_secret','Secret'],['account_id','Merchant/logistics account ID']], can:['Create package proof packets','Track fulfillment references','Collect independent proof evidence'], webhook:'/api/providers/webhook/amazon_flex', statuses:{created:'Assigned',out_for_delivery:'In Progress',delivered:'Delivered',exception:'Exception'}, template:{provider_order_id:'FLEX-1001',customer:{name:'Amazon Flex Test Customer'},address:'302 E 4th St, Joplin, MO',type:'Package Delivery'}},
  roadie: { access:'Requires approved Roadie API access', required:[['base_url','Roadie API base URL'],['api_key','API key'],['api_secret','Secret'],['account_id','Account ID']], can:['Create gig courier packets','Track pickup/dropoff','Attach proof checklist and photos'], webhook:'/api/providers/webhook/roadie', statuses:{created:'Assigned',accepted:'In Progress',picked_up:'In Progress',delivered:'Delivered',cancelled:'Exception'}, template:{provider_order_id:'ROADIE-1001',customer:{name:'Roadie Test Customer'},address:'302 E 4th St, Joplin, MO',type:'Courier'}},
  shipt: { access:'Requires approved Shipt partner API access', required:[['base_url','Shipt API base URL'],['api_key','API key'],['api_secret','Secret'],['account_id','Store/account ID']], can:['Create grocery/retail service packets','Track shopper/delivery stages','Capture customer acceptance proof'], webhook:'/api/providers/webhook/shipt', statuses:{created:'Assigned',shopping:'In Progress',delivered:'Delivered',cancelled:'Exception'}, template:{provider_order_id:'SHIPT-1001',customer:{name:'Shipt Test Customer'},address:'302 E 4th St, Joplin, MO',type:'Grocery Delivery'}},
  usps: { access:'Requires USPS API/Web Tools or USPS Developer access', required:[['base_url','USPS API base URL'],['api_key','USPS API key/user ID'],['account_id','Mailer/account ID']], can:['Build tracking URL','Import carrier status events','Create proof packet from tracking number'], webhook:'/api/providers/webhook/usps', statuses:{pre_shipment:'Assigned',in_transit:'In Progress',delivered:'Delivered',exception:'Exception'}, template:{tracking_number:'9400111899223856789012',carrier:'USPS',type:'Carrier Tracking'}},
  fedex: { access:'Requires FedEx Developer API credentials', required:[['base_url','FedEx API base URL'],['api_key','Client ID / key'],['api_secret','Client secret'],['account_id','FedEx account number']], can:['Build tracking URL','Import carrier tracking events','Create delivery proof packets'], webhook:'/api/providers/webhook/fedex', statuses:{created:'Assigned',in_transit:'In Progress',delivered:'Delivered',exception:'Exception'}, template:{tracking_number:'123456789012',carrier:'FedEx',type:'Carrier Tracking'}},
  ups: { access:'Requires UPS Developer Kit / OAuth credentials', required:[['base_url','UPS API base URL'],['api_key','Client ID'],['api_secret','Client secret'],['account_id','UPS account number']], can:['Build tracking URL','Import tracking events','Create proof packet from UPS tracking'], webhook:'/api/providers/webhook/ups', statuses:{created:'Assigned',in_transit:'In Progress',delivered:'Delivered',exception:'Exception'}, template:{tracking_number:'1Z999AA10123456784',carrier:'UPS',type:'Carrier Tracking'}},
  digital_delivery: { access:'Uses Promote4.me webhook/API key; add platform-specific keys for Fiverr/Upwork-style sources later', required:[['base_url','Source platform or client portal URL'],['api_key','Promote4.me/source API key'],['webhook_secret','Webhook signing secret']], can:['Deliverable URL tracking','Client approval workflow','Revision requested workflow','Final accepted workflow'], webhook:'/api/providers/webhook/digital_delivery', statuses:{submitted:'Submitted',client_review:'Submitted',revision_requested:'Rejected',final_accepted:'Verified'}, template:{provider_order_id:'DIGITAL-1001',digital_delivery_url:'https://example.com/final-deliverable',type:'Online Deliverable',instructions:'Submit deliverable URL, wait for client approval or revision request.'}}
};
function p4meTrackingUrl300(carrier, tracking){ const c=String(carrier||'').toLowerCase(), t=encodeURIComponent(tracking||''); if(!t) return ''; if(c==='usps') return 'https://tools.usps.com/go/TrackConfirmAction?tLabels='+t; if(c==='fedex') return 'https://www.fedex.com/fedextrack/?trknbr='+t; if(c==='ups') return 'https://www.ups.com/track?tracknum='+t; return ''; }
app.get('/api/providers/:providerId/playbook', requireAuth, (req,res)=>{ const id=req.params.providerId; const playbook=P4ME_PROVIDER_PLAYBOOKS_300[id]; if(!playbook) return res.status(404).json({error:'No playbook for provider.'}); res.json({provider_id:id, ...playbook}); });
app.post('/api/carriers/:carrier/track', requireAuth, (req,res)=>{ const carrier=String(req.params.carrier||'').toLowerCase(); const b=req.body||{}; const tracking=b.tracking_number||b.tracking||''; const tracking_url=p4meTrackingUrl300(carrier, tracking); const status=b.status||'in_transit'; const mapped=(P4ME_PROVIDER_PLAYBOOKS_300[carrier]?.statuses||{})[status]||'In Progress'; res.json({ok:true,carrier:carrier.toUpperCase(),tracking_number:tracking,tracking_url,status,mapped_status:mapped,events:[{status:'created',label:'Tracking reference received',time:now()},{status, label:'Current carrier status preview', time:now()}]}); });
app.post('/api/carriers/:carrier/proof-packet', requireAuth, (req,res)=>{ const carrier=String(req.params.carrier||'').toLowerCase(); const tracking=req.body?.tracking_number||''; const payload={...(req.body||{}), carrier:carrier.toUpperCase(), tracking_number:tracking, tracking_url:p4meTrackingUrl300(carrier, tracking), type:'Carrier Tracking', title:carrier.toUpperCase()+' tracking proof '+tracking, provider_order_id:tracking}; const job=p4meProviderCreateJob(req.user.tenant_id, carrier, payload); res.json({ok:true,job}); });
app.post('/api/digital-delivery/preview', requireAuth, (req,res)=>{ const b=req.body||{}; const states=['Submitted','Client Review','Revision Requested','Final Accepted']; res.json({ok:true,deliverable_url:b.digital_delivery_url||'https://example.com/final-deliverable',states,current:b.status||'Submitted',checklist:[{label:'Deliverable URL submitted',done:true},{label:'Client review requested',done:['Client Review','Revision Requested','Final Accepted'].includes(b.status)},{label:'Revision resolved',done:b.status==='Final Accepted'},{label:'Final accepted',done:b.status==='Final Accepted'}]}); });
app.post('/api/digital-delivery/proof-packet', requireAuth, (req,res)=>{ const job=p4meProviderCreateJob(req.user.tenant_id, 'digital_delivery', {...(req.body||{}), type:'Online Deliverable', digital_delivery_url:req.body?.digital_delivery_url||'', title:req.body?.title||'Digital deliverable proof packet'}); res.json({ok:true,job}); });
`;
  server = server.replace("app.post('/api/auth/register'", endpoints + "\napp.post('/api/auth/register'");
}
fs.writeFileSync('server/index.js', server);

let app = fs.readFileSync('src/ProductApp.jsx', 'utf8');
if (!has(app, 'function ProviderPlaybooks300')) {
  const components = `
function ProviderPlaybooks300({setNotice}){const [catalog,setCatalog]=useState([]),[provider,setProvider]=useState('doordash'),[book,setBook]=useState(null),[tracking,setTracking]=useState({carrier:'usps',tracking_number:''}),[trackingResult,setTrackingResult]=useState(null),[digital,setDigital]=useState({digital_delivery_url:'https://example.com/final-deliverable',status:'Submitted'}),[digitalResult,setDigitalResult]=useState(null);async function loadCatalog(){try{const c=await api('/api/providers/catalog');setCatalog(c.providers||[])}catch(e){setNotice(e.message)}}async function loadBook(id=provider){try{setBook(await api('/api/providers/'+id+'/playbook'))}catch(e){setBook(null);setNotice(e.message)}}useEffect(()=>{loadCatalog()},[]);useEffect(()=>{loadBook(provider)},[provider]);async function carrierPreview(){try{setTrackingResult(await api('/api/carriers/'+tracking.carrier+'/track',{method:'POST',body:JSON.stringify(tracking)}))}catch(e){setNotice(e.message)}}async function carrierPacket(){try{const r=await api('/api/carriers/'+tracking.carrier+'/proof-packet',{method:'POST',body:JSON.stringify(tracking)});setNotice('Carrier proof packet created: '+r.job.id)}catch(e){setNotice(e.message)}}async function digitalPreview(){try{setDigitalResult(await api('/api/digital-delivery/preview',{method:'POST',body:JSON.stringify(digital)}))}catch(e){setNotice(e.message)}}async function digitalPacket(){try{const r=await api('/api/digital-delivery/proof-packet',{method:'POST',body:JSON.stringify(digital)});setNotice('Digital proof packet created: '+r.job.id)}catch(e){setNotice(e.message)}}return <div className="provider-playbooks"><section className="panel wide"><h3>Platform Playbooks</h3><p className="hint">Provider-specific credentials, webhooks, status mappings, API access notes, and test payload templates.</p><div className="provider-tabs">{catalog.map(p=><button key={p.id} className={provider===p.id?'active':''} onClick={()=>setProvider(p.id)}>{p.name}</button>)}</div>{book&&<div className="playbook-grid"><article className="playbook-card access"><strong>API access</strong><span>{book.access}</span><em>requires approved API access</em></article><article className="playbook-card"><strong>Required credentials</strong>{book.required.map(([key,label])=><p key={key}><code>{key}</code> {label}</p>)}</article><article className="playbook-card"><strong>What this platform can do</strong><ul>{book.can.map(x=><li key={x}>{x}</li>)}</ul></article><article className="playbook-card"><strong>Webhook example</strong><code>{location.origin}{book.webhook}</code></article><article className="playbook-card"><strong>Status mapping</strong>{Object.entries(book.statuses||{}).map(([a,b])=><p key={a}><code>{a}</code> → <b>{b}</b></p>)}</article><article className="playbook-card"><strong>Test payload</strong><pre>{JSON.stringify(book.template,null,2)}</pre></article></div>}</section><section className="panel wide"><h3>USPS / FedEx / UPS tracking workflow</h3><div className="carrier-form"><select value={tracking.carrier} onChange={e=>setTracking({...tracking,carrier:e.target.value})}><option value="usps">USPS</option><option value="fedex">FedEx</option><option value="ups">UPS</option></select><input placeholder="Tracking number" value={tracking.tracking_number} onChange={e=>setTracking({...tracking,tracking_number:e.target.value})}/><button className="secondary" onClick={carrierPreview}>Preview status</button><button className="primary" onClick={carrierPacket}>Create proof packet</button></div>{trackingResult&&<div className="tracking-preview"><strong>{trackingResult.carrier} · {trackingResult.mapped_status}</strong><a href={trackingResult.tracking_url} target="_blank">Open carrier tracking</a>{trackingResult.events.map(e=><p key={e.status}>{e.label} · {e.time}</p>)}</div>}</section><section className="panel wide"><h3>Digital delivery workflow</h3><div className="carrier-form"><input placeholder="Deliverable URL" value={digital.digital_delivery_url} onChange={e=>setDigital({...digital,digital_delivery_url:e.target.value})}/><select value={digital.status} onChange={e=>setDigital({...digital,status:e.target.value})}><option>Submitted</option><option>Client Review</option><option>Revision Requested</option><option>Final Accepted</option></select><button className="secondary" onClick={digitalPreview}>Preview approval flow</button><button className="primary" onClick={digitalPacket}>Create digital proof packet</button></div>{digitalResult&&<div className="tracking-preview"><strong>{digitalResult.current}</strong><a href={digitalResult.deliverable_url} target="_blank">Open deliverable</a>{digitalResult.checklist.map(x=><p key={x.label}>{x.done?'✓':'○'} {x.label}</p>)}</div>}</section></div>}
`;
  app = app.replace('function PublicSite({ plans, onAuthed }) {', components + '\nfunction PublicSite({ plans, onAuthed }) {');
}
if (!has(app, '<ProviderPlaybooks300')) {
  app = app.replace('<ProviderNetworks298 setNotice={setNotice}/>', '<ProviderNetworks298 setNotice={setNotice}/><ProviderPlaybooks300 setNotice={setNotice}/>');
}
fs.writeFileSync('src/ProductApp.jsx', app);

let css = fs.readFileSync('src/styles.css', 'utf8');
if (!has(css, 'P4ME 3.0.0 PROVIDER PLAYBOOKS')) css += `
/* P4ME 3.0.0 PROVIDER PLAYBOOKS */
.provider-playbooks{display:grid;gap:16px;grid-column:1/-1}.provider-tabs{display:flex;gap:8px;overflow:auto;padding:4px 0 12px}.provider-tabs button{white-space:nowrap;border:1px solid rgba(125,211,252,.24);background:#102033;color:#eaf3ff;border-radius:999px;padding:9px 13px}.provider-tabs button.active{background:#0ea5e9;color:#fff}.playbook-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:12px}.playbook-card{background:rgba(255,255,255,.045);border:1px solid rgba(125,211,252,.18);border-radius:18px;padding:16px;display:grid;gap:8px}.playbook-card.access{background:linear-gradient(135deg,rgba(234,179,8,.13),rgba(14,165,233,.08));border-color:rgba(234,179,8,.35)}.playbook-card em{justify-self:start;background:rgba(234,179,8,.20);color:#fde68a;border-radius:999px;padding:5px 9px;font-style:normal;font-size:12px}.playbook-card code,.playbook-card pre{background:#020817;color:#7dd3fc;border-radius:10px;padding:8px;white-space:pre-wrap;overflow:auto}.playbook-card p{margin:0;color:#d4e7f8}.playbook-card ul{margin:0;padding-left:18px;color:#d4e7f8}.carrier-form{display:grid;grid-template-columns:180px minmax(220px,1fr) auto auto;gap:10px}.tracking-preview{margin-top:12px;background:rgba(14,165,233,.08);border:1px solid rgba(125,211,252,.22);border-radius:16px;padding:14px;display:grid;gap:8px}.tracking-preview a{color:#7dd3fc}@media(max-width:900px){.carrier-form{grid-template-columns:1fr}.provider-tabs{position:sticky;top:62px;background:#07111f;z-index:10}.playbook-grid{grid-template-columns:1fr}.provider-playbooks .panel{padding:14px!important}}
`;
fs.writeFileSync('src/styles.css', css);

console.log('Promote4.me 3.0.0 provider playbooks, carrier tracking, and digital delivery workflow applied.');
